[project]
name = "astros"
version = "0.1.0"
description = "Static astrology and sky-state JSON publisher for GitHub Pages"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "pyswisseph>=2.10.3.2",
]

[tool.uv]
cache-dir = ".uv-cache"
package = false
